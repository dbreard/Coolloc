{% extends"dashboard/layout-dashboard.html.twig" %}



{% block headtitle %}
    Gestion Users | CoolLoc, la colocation cool !
{% endblock%}



{% block nav %}
    {# insertion des barre de navigation #}
    {% include 'dashboard/required/nav.html.twig' ignore missing  %}
{% endblock %}



{% block title %}

    <div class="row">
        <div class="col-lg-12">
            <h1 class="page-header">
                Gestion Utilisateurs
            </h1>
            <ol class="breadcrumb">
                <li class="active">
                    <i class="fa fa-dashboard"></i> Dashboard / Gestion utilisateurs cherchant une colocation
                </li>
            </ol>
        </div>
    </div>

{% endblock %}



{% block content %}

    <a href="{{ path ('gerer-user-colocations') }}" class="btn btn-warning"><i class="fa fa-reply"></i> Retour</a>

    <h2 class="">Détail du profil<small> options de l'utilisateur :</small> </h2>

    <div class="table-responsive">
        <table class="table table-hover table-striped col-md-12">
            <thead>
            <tr>
                <th class="text-center">Prénom</th>
                <th class="text-center">Nom</th>
                <th class="text-center">Photo</th>
                <th class="text-center">Acces handicapé</th>
                <th class="text-center">Fumeur</th>
                <th class="text-center">Animal</th>
                <th class="text-center">Composition</th>
                <th class="text-center">Meublé</th>
                <th class="text-center">Jardin</th>
                <th class="text-center">Balcon</th>
                <th class="text-center">parking</th>
            </tr>
            </thead>
                <tr>

                        <td class="text-center">{{ detailsUser.user.firstname }}</td>
                        <td class="text-center">{{ detailsUser.user.lastname }}</td>
                        <td class="text-center"><img src="{{ detailsUser.user.profil_picture }}" alt="Miniature photo de profil" height="200"></td>
                        <td class="text-center">{{ detailsUser.user.handicap_access }}</td>
                        <td class="text-center">{{ detailsUser.user.smoking }}</td>
                        <td class="text-center">{{ detailsUser.user.animals }}</td>
                        <td class="text-center">{{ detailsUser.user.sex_roommates }}</td>
                        <td class="text-center">{{ detailsUser.user.furniture }}</td>
                        <td class="text-center">{{ detailsUser.user.garden }}</td>
                        <td class="text-center">{{ detailsUser.user.balcony }}</td>
                        <td class="text-center">{{ detailsUser.user.parking }}</td>
                 </tr>
        </table>
    </div>

    <h2><small> Choix de l'utilisateur :</small> </h2>

    <div class="table-responsive">
        <table class="table table-hover table-striped col-md-12">
            <tr>
                <th class="col-md-2">Equipement</th>
                {% if detailsUser.user.equipment == '' %}
                    <td>Non défini par l'utilisateur</td>
                {% else %}
                    <td>{{ detailsUser.user.equipment }}</td>
                {% endif %}
            </tr>
            <tr>
                <th>Quartier</th>
                {% if detailsUser.user.district == '' %}
                    <td>Non défini par l'utilisateur</td>
                {% else %}
                    <td>{{ detailsUser.user.district }}</td>
                {% endif %}
            </tr>
            <tr>
                <th>Centres d'interet</th>
                {% if detailsUser.user.hobbies == '' %}
                    <td>Non défini par l'utilisateur</td>
                {% else %}
                    <td>{{ detailsUser.user.hobbies }}</td>
                {% endif %}
            </tr>
            <tr>
                <th>Profil du membre</th>
                {% if detailsUser.user.member_profil == '' %}
                    <td>Non défini par l'utilisateur</td>
                {% else %}
                    <td>{{ detailsUser.user.member_profil }}</td>
                {% endif %}
            </tr>
        </table>
    </div>



{% endblock %}